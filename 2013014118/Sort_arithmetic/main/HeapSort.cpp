//#include"HeapSort.h"
/*
int main()
{
    H h;
    Init_H(h);
    Put_H(h);
    cout << h.length << endl;
    for(int i = 0;i<h.length;i++)
    {
        cout << h.arr[i] << " ";
    }
    cout << endl;
    HeapSort cp;
    cp.heapSort(h);
    for(int i = 0;i<h.length;i++)
    {
        cout << h.arr[i] << " ";
    }
    system("pause");
    return 0;
}
*/
